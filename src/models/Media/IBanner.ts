export interface IBanner {
  bannerTitle?: string;
  bannerText?: string;
  bannerDesc?: string;
  buttonText?: string;
  buttonGoto?: string;
  isButton?: boolean;
  picture: string;
  variant?: string;
}
